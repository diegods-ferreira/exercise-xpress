export type ColorScheme = 'light' | 'dark';

export type AppSettings = {
  showWelcomeScreen?: boolean;
  weight: 'kilogram' | 'pound';
  distance: 'meters' | 'miles';
  bodyMeasurements: 'centimeters' | 'inches';
};
